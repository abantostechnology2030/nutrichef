node_modules/
.env
*.db
*.db-journal
*.db-wal
*.db-shm
uploads/*
!uploads/.gitkeep
.DS_Store
npm-debug.log*

# archivos/ SI se versiona (a diferencia de NutriIA, de donde se heredo este ignore).
# Ahi viven logotipo.png y favicon.png: los UNICOS originales de la marca. Ignorarlos
# significaba que existian solo en el disco de una maquina. public/img/ solo tiene
# derivados mas chicos, asi que no sirven de respaldo.
